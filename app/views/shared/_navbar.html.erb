<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <%= link_to "Articles App", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <% if user_signed_in? %>
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "Nouvel article", new_article_path, class: "nav-link #{'active' if current_page?(new_article_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Profil", edit_user_registration_path, class: "nav-link #{'active' if current_page?(edit_user_registration_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %></li>
      </ul>
      <% end %>
      <% if !user_signed_in? %>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <%= link_to "Inscription", new_user_registration_path, class: "nav-link #{'active' if current_page?(new_user_registration_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Connexion", new_user_session_path, class: "nav-link #{'active' if current_page?(new_user_session_path)}" %>
        </li>
      </ul>
      <% end %>
    </div>
  </div>
</nav>